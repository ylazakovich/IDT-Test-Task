package tests;

import framework.utils.Log;
import org.testng.Assert;

public class CustomAssert extends Assert {
    private static Log log = Log.getInstance();

    //TODO assert for last step (verify 10 companies)
}
